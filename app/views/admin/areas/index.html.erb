<div class="container px-5 px-sm-0">
 <div class="row" style="margin-top:25px;">
  <div class="col-sm-10 px-sm-0 mx-auto mb-5">
      <h2><span class="bg-light">エリア一覧・追加<span></h2>
      </div>
      <div class="mt-4">
        <%= form_with model: @area, url: admin_areas_path, local:true do |f| %>
          <div class="form-group mb-5">
              <%= f.label :"エリア名" %>
              <%= f.text_field :name, placeholder: "エリア名" %>
              <%= f.submit '新規登録', class: 'btn btn-success' %>
          </div>
        <% end %>
      </div>


       <div class="col-sm-7 px-sm-0 mx-auto mb-5">
        <table class="table table-hover">
         <thead class="table-active">
         <tr>
          <th></th>
          <th>エリア</th>
          <th colspan="3"></th>
        </tr>
          </thead>
        <tbody>
         <% @areas.each do |area| %>
         <tr>
           <td><%= area.name %></td>
           <td><%= link_to '編集する', edit_admin_area_path(area.id), class: 'btn btn-sm btn-success' %></td>
         </tr>
       <% end %>
   </tbody>
</table>
