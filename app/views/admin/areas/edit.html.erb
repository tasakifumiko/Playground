<div class="mt-4">
  <div class="col-md-8">

    <h4><span class="bg-light">エリア情報編集<span></h4>
    <div class="mt-4">
      <%= form_with model: @area, url: admin_area_path, method: :patch, local:true do |f| %>
        <div class="form-group">
            <%= f.label :name, "エリア名" %>
            <%= f.text_field :name  %>
            <%= f.submit '変更を保存', class: 'btn btn-success' %>
        </div>
      <% end %>
    </div>
  </div>
</div>