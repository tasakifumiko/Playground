<div class="container">
 <div class="row">
  <div class="col-sm-12 col-md-11 col-lg-8  mx-auto">
   <br>
     <h4><span class="bg-light">あなたのブックマーク一覧<span></h4>
      <table class='table'>
       <thead class="table table-light">
        <tr>
          <th>遊び場名</th>
          <th>user name</th>
          <th>エリア</th>
          <th>ジャンル</th>
        </tr>
       </thead>
         <tbody>
           <% @bookmarks.each do |bookmark| %>
            <tr>
              <td><%= link_to (bookmark.post.playground), post_path(bookmark.post.id) %></td>
              <td><%= bookmark.user.user_name %></td>
              <td><%= bookmark.post.area.name %></td>
              <td><%= bookmark.post.genre.name  %></td>
              <!--<td><%# if user.is_user_deleted %>-->
              <!--      <div class="text-danger">退会済</div>-->
              <!--    <%# else %>-->
              <!--      <div class="text-success">有効会員</div>-->
              <!--    <%# end %>-->
              <!--</td>-->
            </tr>
           <% end %>
　       </tbody>
      </table>
    <!--<div class="col-md-5 mx-auto"><%#= paginate @customers %></div>-->
  </div>
</div>


