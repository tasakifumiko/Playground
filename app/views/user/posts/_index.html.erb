<table class='table table-hover table-inverse'>
<div class="card-list-container py-5">
  <% posts.each do |posts| %>
  <div class="card w-50 mx-auto mt-4 shadow-lg">
    <%= link_to(posts.user) do %>
    <%= image_tag posts.image, size:'100x100' %>
    <% end %>
    <div class="card-body">
      <div class="row">
        <div class="col-2">
          <%= image_tag posts.user.get_profile_image(100,100), class:"mt-3 rounded-circle" %>
          </div>
   　　     <div class="col-10">
        　  <p class="card-title">
        　    
        　    <%= link_to(post_path(post_image.id), class:"text-dark") do %>
              <strong><i class="far fa-flag"></i><span> <%= post_image.shop_name %></span></strong>
            <% end %>
    
  <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>紹介文</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    
      <tr>
        <td>
          
        </td>
        <td><%= posts.playground %></td>
        <td><%= posts.title %></td>
        <td><%= posts.text %></td>
        <!--<td class=<%#= "book_"+book.id.to_s+"_favorite" %>><%#= render 'favorites/favorite', book: book %></td>
        <td>コメント数：<%#= book.book_comments.count %></td>
        </td>
      </tr>
    <% end %>
  </tbody>-->
    
  </div>
</div>
</table>
